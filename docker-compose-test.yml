version: '3.5'
services:
  testapp:
    image: node
    volumes:
      - ../testapp:/testapp
    entrypoint: ["/bin/sh", "-c", "cd /testapp && npm install chai chai-http mocha && mocha"]
    networks:
      - devops
networks:
  devops:
    external: true